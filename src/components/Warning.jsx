function Warning() {
  return (
    <div>
      <p>
        The page could not be loaded because the window width is less than
        900px. Please resize the window.
      </p>
    </div>
  );
}

export default Warning;
